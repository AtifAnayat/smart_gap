name: smart_gap
description: Intelligent spacing widget that auto-detects direction (Row/Column/Flex). No more SizedBox confusion! Use Gap(16) everywhere.
version: 1.0.0
homepage: https://github.com/AtifAnayat/smart_gap
repository: https://github.com/AtifAnayat/smart_gap
issue_tracker: https://github.com/AtifAnayat/smart_gap/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0